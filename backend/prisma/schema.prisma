generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Paste {
  id        String   @id
  content   String
  expiresAt DateTime?
  maxViews  Int?
  views     Int      @default(0)
  createdAt DateTime @default(now())
}
